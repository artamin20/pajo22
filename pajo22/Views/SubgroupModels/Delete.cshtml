@model pajo22.Models.SubgroupModels

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/head_foot.cshtml";
}

<div class="max-w-[1440px] mx-auto px-3">
    <div class="bg-white shadow-xl my-5 lg:my-10 rounded-xl md:rounded-2xl p-3 md:p-5">
        <div class="text-2xl md:text-3xl opacity-70 mb-3 text-center">
            حذف
        </div>

        <div>
            <hr />
            <dl class="grid grid-cols-1 md:grid-cols-2 gap-5">
                <div class="mb-4">
                    <dt class="text-sm font-semibold text-slate-700 mb-2">نام زیرگروه</dt>
                    <dd class="text-sm">@Html.DisplayFor(model => model.Name)</dd>
                </div>

                <div class="mb-4">
                    <dt class="text-sm font-semibold text-slate-700 mb-2">نام گروه</dt>
                    <dd class="text-sm">@Html.DisplayFor(model => model.GroupModels.Name)</dd>
                </div>
            </dl>

            <!-- Warning message -->
            <div class="text-red-500 mb-3">
                حذف کردن این گروه باعث می‌شود تمام زیرگروه‌ها و محصولات مرتبط با آن حذف شوند. پیشنهاد می‌دهیم قبل از حذف، ابتدا زیرگروه‌ها و محصولات را غیرفعال کنید.
            </div>


            <form asp-action="Delete" onsubmit="return confirmDelete()">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="block w-full rounded-lg bg-red-500 text-white text-sm font-bold px-6 py-2 font-normal text-center mb-4">حذف</button>
                <a asp-action="Index" class="block w-full rounded-lg bg-gray-500 text-white text-sm font-bold px-6 py-2 font-normal text-center">بازگشت به لیست</a>
            </form>
        </div>
    </div>
</div>

<script>
    function confirmDelete() {
        return confirm("حدف کردن این گروه باعث می‌شود تمام محصولات و زیرگروه‌های مرتبط به آن تا ابد حذف شوند. پیشنهاد می‌دهیم  به جای حذف کردن غیرفعال کنید. آیا مطمئن هستید که می‌خواهید ادامه دهید؟");
    }
</script>
